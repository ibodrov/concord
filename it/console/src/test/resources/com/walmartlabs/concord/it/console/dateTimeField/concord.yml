configuration:
  runtime: "concord-v2"

flows:
  default:
    - form: myForm
      fields:
        - dateField: { type: "date" }
        - dateTimeField: { type: "dateTime" }
    - log: "myForm: ${myForm}"
