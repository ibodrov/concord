configuration:
  runtime: "concord-v2"

flows:
  default:
  - try:
    - task: http
      in:
       method: GET
       url: "http://dummy/server/get/request"
       response: "json"
      out: rsp
    error:
      - ${log.error("server not exists")}
      - throw: "HTTP request failed"
