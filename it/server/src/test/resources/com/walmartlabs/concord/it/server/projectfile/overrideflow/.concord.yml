configuration:
  arguments:
    name: "world"

  runtime: "concord-v2"

flows:
  main:
  - call: myOtherFlow

  myOtherFlow:
  - log: Bye, ${name}

profiles:
  test:
    flows:
      myOtherFlow:
      - log: Hello, ${name}
