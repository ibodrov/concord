configuration:
  runtime: "concord-v2"

flows:
  onPush:
    - log: "Hello, ${requestInfo.headers.Host}!"

triggers:
  - github:
      entryPoint: "onPush"
      version: 2
      conditions:
        type: "push"
