imports:
  - git:
      url: "{{gitUrl}}"
      version: "{{version}}"
      dest: "concord"

flows:
  default:
    - log: "Hello, ${name}!"
    - call: flowFromTemplate

configuration:
  runtime: "concord-v2"
  arguments:
    name: "Concord"
