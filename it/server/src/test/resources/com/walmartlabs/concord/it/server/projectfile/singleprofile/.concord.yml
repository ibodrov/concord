configuration:
  arguments:
    name: "stranger"
    nested:
      val: "xyz12345abc"

  runtime: "concord-v2"

flows:
  main:
  - log: Hello, ${name}
  - log: ${nested.val}

profiles:
  test:
    configuration:
      arguments:
        name: "world"
        nested:
          val: "xyz54321abc"
