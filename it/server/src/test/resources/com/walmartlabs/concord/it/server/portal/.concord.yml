configuration:
  arguments:
    name: "stranger"
    x: 0

  runtime: "concord-v2"

flows:
  main:
    - log: "Hello, ${name}"
    - log: "x=${x}"

profiles:
  test1:
    configuration:
      arguments:
        name: "world"
  test2:
    configuration:
      arguments:
        x: 123
