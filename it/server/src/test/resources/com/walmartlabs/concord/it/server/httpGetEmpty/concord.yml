configuration:
  runtime: "concord-v2"

flows:
  default:

  - task: http
    in:
      method: GET
      url: ${url}
      response: string
    out: rsp

  - log: "Success response: ${rsp.statusCode >= 200 and rsp.statusCode < 300}"
  - log: "Content is NULL: ${rsp.content == null}"
