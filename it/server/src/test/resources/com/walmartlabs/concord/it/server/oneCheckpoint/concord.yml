configuration:
  runtime: "concord-v2"
  runner:
    events:
      recordTaskOutVars: true # test uses out vars to verify checkpoints

flows:
  default:
    - log: "aaa before"
    - checkpoint: "one"
    - log: "bbb after"