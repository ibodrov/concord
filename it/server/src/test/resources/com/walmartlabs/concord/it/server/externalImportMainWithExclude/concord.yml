imports:
  - git:
      url: "{{gitUrl}}"
      exclude:
        - "{{exclude}}"

flows:
  default:
    - log: "Hello, ${name}!"
    - call: flowFromTemplate

configuration:
  runtime: "concord-v2"
  arguments:
    name: "Concord"
