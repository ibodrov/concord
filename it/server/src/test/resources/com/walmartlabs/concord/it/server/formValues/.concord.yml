configuration:
  entryPoint: main
  runtime: "concord-v2"

flows:
  main:
  - call: sub

  sub:
  - set:
      message: "Hello"
  - form: myForm
  - log: "${message}, ${myForm.name}"

forms:
  myForm:
  - name: {type: "string"}
