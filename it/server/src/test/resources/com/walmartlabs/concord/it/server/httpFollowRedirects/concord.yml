flows:
  default:
  - task: http
    in:
      method: GET
      url: ${url}
      response: string
      followRedirects: false
    out: rsp
  - log: "Response status code: ${rsp.statusCode}"

configuration:
  runtime: "concord-v2"
  arguments:
    url: "http://localhost:8001"
