configuration:
  runtime: "concord-v2"

flows:
  default:
  - form: myForm
  - log: "Skills -> ${myForm.skills}"

forms:
  myForm:
  - skills: { type: "string*", allow: ["css", "react", "angular"] }

