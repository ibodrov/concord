configuration:
  runtime: "concord-v2"

flows:
  default:

  - task: concord
    in:
      action: start
      payload: payload
      sync: true
      arguments:
        baseUrl: ${baseUrl}
        orgName: ${orgName}
        inventoryName: ${inventoryName}
        queryName: ${queryName}

  - log: "Done! "
